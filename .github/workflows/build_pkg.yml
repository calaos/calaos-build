name: Repository Dispatch
on:
  repository_dispatch:
    types: [build_pkg]
jobs:
  myEvent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make-pkgs-${{ github.event.pkgname }}
        run: make build-${{ github.event.pkgname }}
